"""Tests for multiple decision variables (DECIDE x, y, ...).

No tests populated yet. Add tests here for queries like:
    DECIDE x IS BOOLEAN, y IS INTEGER
    SUCH THAT SUM(x * weight) + SUM(y * cost) <= 100
"""
