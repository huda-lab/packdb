"""Tests for SQL subqueries beyond constraint RHS.

No tests populated yet. Subquery-in-RHS is covered by test_cons_subquery.py.
Add tests here for subqueries in other positions (FROM, WHERE, SELECT).
"""
