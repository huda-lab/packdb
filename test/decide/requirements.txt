highspy>=1.5.0
pytest>=7.0.0
# gurobipy  # optional — install separately with a valid Gurobi license
# Note: packdb (installed from tools/pythonpkg) is used instead of vanilla duckdb
# because both share pybind11 types and cannot coexist in one process.
